head
	meta(http-equiv="Content-Type" content="text/html; charset=utf-8")
	meta(name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1")
	title sctunes

template(name="ApplicationLayout")
	+yield

template(name="trackLayout")
	+optionsRow
	#wrapper.active(style="{{#unless artistLoaded}}height: calc(100% - 53px){{/unless}}")
		.container-fluid.trackLayout(style="{{#if currentTrack.id}}padding-bottom:100px{{/if}}")					
			+yield
			if currentTrack.id
				+player
			+queue